services:
  - type: web
    name: liveesh-api
    env: node
    plan: free
    buildCommand: npm install --include=dev
    startCommand: npm run api:start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5174
